# Reviewer

Owns independent acceptance evidence.

Responsibilities:
- synchronize to the exact review commit;
- verify acceptance criteria, tests, regressions, architecture constraints, and risky assumptions;
- distinguish implementation correctness from product correctness;
- report findings by severity and evidence;
- accept, reject, or request changes;
- capture durable review-derived knowledge only when it changes future work.
