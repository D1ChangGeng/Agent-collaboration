---
id: HANDOFF-2026-09-10-MAIN-V040-SYNC
task: P1-00-REPO-SYNC
from_role: coordinator
to_role: next-session-management
created_at: 2026-09-10T14:05:00+08:00
status: ready
---

# Objective

将管理工作区纳入正式 Git 持久化，整理本地、GitHub 和 1302-1 远程源码状态，统一准确远程路径为 `/home/changgeng/Agent-collaboration`，并清理旧拼写路径。

# Actual result

- 父仓库 `.gitignore` 中的 `agent-collabration/*` 已移除，管理工作区现在由 Git 跟踪。
- `RFC_TEMP_SOURCE/` 全量纳入 Git。
- 新提交：`24abbd68df0241d6f680ee974a00058d3dd8539b`，标题 `Preserve collaboration workspace and runtime RFC baseline`。
- 已推送 GitHub `main`。
- 1302-1 已从 GitHub `main` 建立准确路径 `/home/changgeng/Agent-collaboration`，并验证管理工作区与 RFC 文件存在。
- 旧路径 `/home/changgeng/Agent-collabration` 已在新路径验证完成后删除。

# Files / artifacts changed

- `.gitignore`
- `agent-collabration/`（32 个管理工作区文件）
- `RFC_TEMP_SOURCE/`（56 个 RFC/设计/验证文件）
- 本 handoff 文件

# Verification

- 本机 `main` 工作树干净，HEAD 与 `origin/main` 均为 `24abbd68df0241d6f680ee974a00058d3dd8539b`。
- 本机 `VERSION=0.4.0`。
- GitHub default branch 为 `main`，当前 SHA 为 `24abbd68df0241d6f680ee974a00058d3dd8539b`。
- GitHub `v0.4.0` Release 仍为正式发布、非 draft、非 prerelease；tag 仍锚定发布提交 `d40435513d744e8e0f3fbb214bd78bf8c6e2e5ea`，main 上的 `24abbd6` 是其后的 release-handoff/管理基线提交。
- 本机 Workspace validate、Route list、Skill validate、RFC schema fixture、RFC boundary surface、93 项 unittest（4 项因 Windows symlink 权限跳过）均通过。
- 1302-1 新路径 `main` 工作树干净，HEAD 与其本地 `origin/main` 均为 `24abbd68df0241d6f680ee974a00058d3dd8539b`，`VERSION=0.4.0`，管理文件 32 个，RFC 文件 56 个。
- 旧路径删除后确认不存在；删除前已做隔离占用检查，没有发现旧路径的外部 cwd/fd/lsof 持有者。

# Unresolved items and risks

- SSH 每次连接仍报告 remote port forwarding listen port 7890 失败；不影响本次 Git/路径同步，但转发能力仍未验证。
- 1302-1 宿主 PATH 默认 Node 18，Node 24/pnpm/Codex/OpenCode 需显式加载 nvm PATH。
- Temporal 存在 1.29.1 healthy 与 1.29.0 unhealthy 容器；Temporal/Node/Driver conformance 尚未开始。
- 管理工作区保留了内部环境基线 handoff；它不是凭据，但包含内网地址、主机名、远程路径和运行状态。
- `v0.4.0` annotated tag 不是当前 main HEAD 的直接指针；当前 main 属于 v0.4.0 发布线。
- 远程最后一次 `git ls-remote` 因 TLS 短暂断开失败，但新 checkout 来源为同一 GitHub main，且本地 origin/main 与 HEAD 已直接验证一致。

# Repository Sync

- Branch: `main`
- Base commit: `8033d306dbc50f26ee49d5591f871fc73e684a27`
- Content sync commit: `24abbd68df0241d6f680ee974a00058d3dd8539b`
- Handoff commit: `e8cb46e` (the commit that first recorded this file)
- Working tree: clean at the handoff commit; the final metadata correction is a follow-up commit on `main`
- Push: yes, GitHub `main` was updated to the handoff commit and will be updated again for this correction
- Receiver sync: remote new checkout must fast-forward to the final correction commit

# Knowledge / decision updates

- 已决定管理工作区不是临时噪声，应作为正式 Git 内容持续保存；因此移除父 `.gitignore` 的管理目录忽略规则。
- `RFC_TEMP_SOURCE/` 作为架构研究输入和验证材料正式保存，但其中 Proposed/not_run 内容仍不能当作 Runtime 已实现证据。
- 准确远程源码路径固定为 `/home/changgeng/Agent-collaboration`；旧 `/home/changgeng/Agent-collabration` 已废弃并删除。

# Required next action

新会话从 `/home/changgeng/Agent-collaboration` 的 `main`、提交 `24abbd6` 开始，先读取本 handoff、目标指令和 Root/Route 合同；继续 P1 前重新核对远程 PATH、Temporal/PostgreSQL/Driver 能力，不把 v0.4.0 setup-only 或 RFC fixture 误标为 Runtime supported。

# Reply required

no
