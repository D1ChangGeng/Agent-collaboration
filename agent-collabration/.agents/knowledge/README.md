# Root Knowledge Plane

`.agents/knowledge/` stores durable project-wide collaboration invariants and
decisions. It does not absorb Route-specific goals, route state, engineer
reports, or the self-evolution knowledge trees already owned by Routes.

Use the installed `self-evolution` workflow for discovery, capture, correction,
retrieval, indexing, and maintenance. ACHP only defines the scope boundary and
the route pointers.
